# Ultra minimal Lambda requirements - KiteConnect only
# Removing heavy packages to ensure KiteConnect installs

# Core trading API - ESSENTIAL
kiteconnect==5.0.1

# Basic HTTP and utilities
requests==2.31.0
python-dotenv==1.0.0