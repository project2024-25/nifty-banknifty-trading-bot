# Lambda deployment requirements - minimal and compatible
# Optimized for AWS Lambda with proven compatibility

# Core trading dependencies
kiteconnect==5.0.1
pandas==2.0.3
numpy==1.24.3

# Basic HTTP for Telegram notifications
requests==2.31.0

# Database
supabase==2.3.0

# Data processing
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Financial calculations (lightweight)
yfinance==0.2.18

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# AWS SDK is available in Lambda runtime
# boto3 - already included